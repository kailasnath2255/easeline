# Set the maximum heap size for the Gradle daemon to 4GB
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError

# Use AndroidX libraries (required for modern Flutter plugins)
android.useAndroidX=true
android.enableJetifier=true

# Specify the Java version for Gradle (use JDK 17 for compatibility)
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.13.11-hotspot

# Enable parallel project execution (improves build speed)
org.gradle.parallel=true

# Enable caching for faster builds
org.gradle.caching=true

# Configure daemon behavior
org.gradle.daemon=true
